{
  "private": true,
  "workspaces": [
    "crates/ferrimock-napi",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter 'ferrimock' build && bun run --filter '@ferrimock/playwright' build"
  },
  "devDependencies": {
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3"
  }
}
