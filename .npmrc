# This repo publishes to and installs from the public npm registry.
#
# A contributor whose user-level ~/.npmrc points at a private mirror would
# otherwise resolve every dependency through it and write those URLs into
# bun.lock, which then 403s for everyone who cannot reach that mirror -- CI
# included. Pinning it here keeps the lockfile portable.
registry=https://registry.npmjs.org/
