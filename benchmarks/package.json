{
  "name": "ferrimock-benchmarks",
  "private": true,
  "type": "module",
  "description": "Cross-tool benchmark suite: ferrimock vs MSW, Mockoon, Prism, json-server, WireMock",
  "scripts": {
    "bench": "node run.mjs",
    "bench:interceptor": "node interceptor.mjs",
    "bench:servers": "node servers.mjs"
  },
  "devDependencies": {
    "@mockoon/cli": "^9.8.0",
    "@stoplight/prism-cli": "^5.16.0",
    "json-server": "1.0.0-beta.15",
    "msw": "^2.15.0"
  }
}
